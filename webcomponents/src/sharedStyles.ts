import { css } from 'lit';

export const sharedStyles = css`
  .missing-profiles {
    font-size: 3rem; 
    --indicator-color: deeppink; 
    --track-color: pink; 
    --track-width: 6px;
  }    
`;
