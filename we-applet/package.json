{
  "name": "we-applet",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "private": true,
  "scripts": {
    "tsc"              : "tsc",
    "dist"             : "rm -rf dist && HAPP_BUILD_MODE=Retail vite build",
    "build"            : "vite dev --clearScreen false",
    "devtest"          : "UI_PORT=$(port) HC_APP_PORT=$(port) HC_ADMIN_PORT=$(port) HAPP_BUILD_MODE='Debug' concurrently \"npm run start:happ\" \"sleep 5 && npm run start:ui\"",
    "devtest:block"    : "UI_PORT=$(port) HC_APP_PORT=$(port) HC_ADMIN_PORT=$(port) HAPP_BUILD_MODE='Debug' APPLET_VIEW='ImportFile' concurrently \"npm run start:happ\" \"npm run start:ui\"",
    "start:happ"       : "RUST_LOG=warn WASM_LOG=debug echo \"pass\" | ../$npm_package_config_bin/hc s --piped -f=$HC_ADMIN_PORT generate ../artifacts/files.happ --run=$HC_APP_PORT -a Files network mem",
    "start:happ:local" : "RUST_LOG=warn WASM_LOG=debug echo \"pass\" | ../$npm_package_config_bin/hc s --piped -f=$HC_ADMIN_PORT generate ../artifacts/files.happ --run=$HC_APP_PORT -a Files network --bootstrap http://127.0.0.1:$BOOT_PORT webrtc ws://127.0.0.1:$SIGNAL_PORT",
    "start:local"      : "UI_PORT=$(port) HC_APP_PORT=$(port) HC_ADMIN_PORT=$(port) HAPP_BUILD_MODE='Debug' concurrently \"npm run start:happ:local\" \"npm run start:ui\"",
    "start:ui"         : "vite --port $UI_PORT --clearScreen false",

    "serve:run"        : "hc run-local-services --bootstrap-port $BOOT_PORT --signal-port $SIGNAL_PORT",
    "network:local2"   : "npm run build && cross-env BOOT_PORT=$(port) SIGNAL_PORT=$(port) concurrently \"npm run serve:run\" \"npm run start:local\" \"sleep 1 && npm run start:local\""
  },
  "devDependencies": {
    "vite": "^4.5.2",
    "vite-plugin-static-copy": "0.16.0",
    "vite-plugin-checker": "^0.5.3",
    "vite-plugin-top-level-await": "=1.5.0",
    "bestzip": "^2.2.0",
    "concurrently": "^5.3.0",
    "deepmerge": "^4.2.2",
    "new-port-cli": "^1.0.0",
    "tslib": "^2.4.0",
    "typescript": "~5.2.0"
  },
  "dependencies": {
    "@files/app": "file:../webapp",
    "@ddd-qc/files": "file:../webcomponents",
    "@mdi/js": "^7.2.0"
  }
}
